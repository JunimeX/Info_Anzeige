<UserControl x:Class="CustomUserControls.borderTextboxWithTitle"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CustomUserControls"
      mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source=".\ressourcen\styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Label Grid.Row="0" Content="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:borderTextboxWithTitle}}, Path=Title}" 
               VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="0" Foreground="Black" VerticalContentAlignment="Bottom" FontSize="11" FontWeight="Bold"/>

        <TextBox Style="{StaticResource RoundCornerTextBox}" Grid.Row="1"
            Text="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:borderTextboxWithTitle}}, Path=Text}" 
            FontSize="11"/>

    </Grid>
</UserControl>
